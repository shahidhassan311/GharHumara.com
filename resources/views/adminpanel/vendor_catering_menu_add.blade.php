@extends('layouts.admin_master_layout')@section('content')    <div class="content custom-scrollbar">        <div class="doc forms-doc page-layout simple full-width">            <!-- HEADER -->            <div class="page-header bg-secondary text-auto p-6 row no-gutters align-items-center justify-content-between">                <h2 class="doc-title" id="content">MENUS</h2>            </div>            <!-- / HEADER -->            <!-- CONTENT -->            <div class="page-content p-6">                <div class="content container-fluid">                    <div class="row">                        {{--<h1>{{ \Illuminate\Support\Facades\Auth::user()->name }}</h1>--}}                        <h1>MENUS</h1>                        <!-- TEXTUAL INPUTS -->                        <div class="col-12 col-md-12">                            <div class="example">                                <div class="description">                                    <div class="description-text" style="width: 100%">                                        <h5 style="display: inline-block">Add Catering</h5>                                    </div>                                    <div class="toggle-source-preview">                                        {{--<i class="icon icon-code-tags"></i>--}}                                        <i class="icon icon-eye-outline"></i>                                    </div>                                </div>                                <div class="source-preview-wrapper">                                    <div class="preview">                                        <div class="preview-elements">                                            <div class="flash-message">                                                @foreach (['danger', 'warning', 'success', 'info'] as $msg)                                                    @if(Session::has('alert-' . $msg))                                                        <p class="alert alert-{{ $msg }}">{{ Session::get('alert-' . $msg) }}                                                            <a href="#" class="close" data-dismiss="alert"                                                               aria-label="close">&times;</a></p>                                                    @endif                                                @endforeach                                            </div>                                            <form action="{{ ($update)? url('/vendor_catering_menus_update') : url('/vendor_catering_menus_store') }}" method="post">                                                {{ csrf_field() }}                                                <input type="hidden" value="{{Request::segment(2)}}" name="url_id">                                                <input type="hidden" value="{{Request::segment(3)}}" name="back_id">                                                    @if($update == true)                                                @else                                                    <div class="form-group">                                                                    <label for="exampleFormControlSelect1">Select Hall</label>                                                                    <select class="form-control" disabled                                                                name="vendor_catering_deal_id"                                                                id="exampleFormControlSelect1">                                                                            @foreach ($vendor_service as $key => $vendor_deals)                                                                                    {{--<option value="{{ $vendor_deals->id }}">{{ $vendor_deals->course_name}}</option>--}}                                                                                    <option value="{{ $vendor_deals->id }}"{{ $vendor_deals->id == Request::segment(2) ? 'selected="selected"' : '' }}>{{ $vendor_deals->course_name }}</option>                                                                                                                            {{--<option value="{{ $halls->id }}" {{($update) ? $hall_section->id == $halls->id : 'selected="selected"' == ''}}>{{ $halls->hall_name }}</option>--}}                                                                            @endforeach                                                                    </select>                                                            </div>                                                @endif                                                <div class="form-group">                                                    <input class="form-control" style="border-bottom: 0.1px solid darkgrey;" type="text" placeholder="Menu Name"                                                           name="menu_name"  value="{{ ($update) ? $vendor_service->menu_name : old('menu_name') }}" id="example-text-input"/>                                                    <label for="example-text-input">Menu Name*</label>                                                    @if ($errors->has('menu_name'))                                                        <span class="help-block alert alert-danger">                                                            <strong>The menu name field is required.</strong>                                                        </span>                                                    @endif                                                </div>                                                <div class="form-group">                                                    <button class="btn btn-danger pull-right" type="submit"                                                            id="example-color-input">Submit                                                    </button>                                                </div>                                            </form>                                        </div>                                    </div>                                </div>                            </div>                        </div>                        <!-- TEXTUAL INPUTS -->                    </div>                </div>            </div>            <!-- CONTENT -->        </div>    </div>@endsection